import type { FC } from "react";

const Sended: FC = () => {
  return (
    <div className="w-full h-full flex justify-center items-center py-96 text-secondary-gray text-4xl font-fire-code-retina">
      email sent 🚀!!!
    </div>
  );
};

export default Sended;
